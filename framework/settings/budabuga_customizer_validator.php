<?php
/**
 * Custom Validators for Customizer
 *
 * @package Budabuga
 * @since 1.00
 */

// Preventing direct script access.
if ( ! defined( 'ABSPATH' ) ) :
	die( 'No direct script access allowed' );
endif;
//
// function validate_numeric_input( $value ) {
// 	$value = intval( $value );
//
// 	if ( empty( $value ) || ! is_numeric( $value ) ) {
// 		return new WP_error( 'required', __( 'You must supply a valid year.', 'budabuga' ) );
// 	}
//
// 	return true;
// }

// add_action( 'customize_register', 'validate_numeric_input' );
